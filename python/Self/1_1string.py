#in python, getopt(args, options, long_options) is used to parse the command line arguments
#args: list of arguments to be parsed, it uses sys.argv[1:] as default
#options: string of short options=>if any option requires an argument, it should be followed by a colon(:) 
#long_options: list of long options=>if any option requires an argument, it should be followed by an equal sign(=)